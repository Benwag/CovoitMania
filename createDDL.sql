CREATE TABLE PREFERENCES (PREFID BIGINT AUTO_INCREMENT NOT NULL, BLABLA VARCHAR(255), CONDUCTEUR VARCHAR(255), DETOUR BIGINT, FUMEUR VARCHAR(255), MUSIC VARCHAR(255), PRIMARY KEY (PREFID))
CREATE TABLE UTILISATEUR (ID BIGINT AUTO_INCREMENT NOT NULL, ADDRESS VARCHAR(255), AGE INTEGER, CITY VARCHAR(255), COORD LONGBLOB, EMAIL VARCHAR(255), FIRSTNAME VARCHAR(255), LASTNAME VARCHAR(255), PASSWORD VARCHAR(255), POSTCODE INTEGER, SCORE BIGINT, PREFERENCE_PREFID BIGINT, PRIMARY KEY (ID))
ALTER TABLE UTILISATEUR ADD CONSTRAINT FK_UTILISATEUR_PREFERENCE_PREFID FOREIGN KEY (PREFERENCE_PREFID) REFERENCES PREFERENCES (PREFID)
