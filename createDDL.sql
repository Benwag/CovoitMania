CREATE TABLE PREFERENCES (PREFID BIGINT NOT NULL, BLABLA VARCHAR(255), CONDUCTEUR VARCHAR(255), DETOUR BIGINT, FUMEUR VARCHAR(255), MUSIC VARCHAR(255), PRIMARY KEY (PREFID))
CREATE TABLE UTILISATEUR (ID BIGINT NOT NULL AUTO_INCREMENT, ADDRESS VARCHAR(255), AGE INTEGER, CITY VARCHAR(255), COORD LONGBLOB, EMAIL VARCHAR(255), FIRSTNAME VARCHAR(255), LASTNAME VARCHAR(255), PASSWORD VARCHAR(255), POSTCODE INTEGER, SCORE BIGINT, PREFERENCE_PREFID BIGINT, PRIMARY KEY (ID))
ALTER TABLE UTILISATEUR ADD CONSTRAINT FK_UTILISATEUR_PREFERENCE_PREFID FOREIGN KEY (PREFERENCE_PREFID) REFERENCES PREFERENCES (PREFID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
